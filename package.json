{
  "name": "animaargenta-site",
  "version": "1.0.0",
  "scripts": {
    "nft:preview": "node scripts/nft-batch.mjs",
    "nft:apply": "node scripts/nft-batch.mjs --apply",
    "verify:visual": "node scripts/check-visual-contract.mjs",
    "build:css": "postcss css/*.css -d dist/css --no-map --env production",
    "build:js": "rollup js/scripts.js --file dist/js/bundle.js --format iife",
    "minify:html": "html-minifier index.html --collapse-whitespace --remove-comments --minify-css true --minify-js true -o dist/index.html",
    "copy:static": "cp -R data favicons gallery nfts pictogramas .well-known dist/ && cp CNAME robots.txt sitemap.xml .nojekyll dist/",
    "build": "npm run verify:visual && rm -rf dist && npm run build:css && npm run build:js && npm run minify:html && npm run copy:static",
    "deploy": "gh-pages -d dist",
    "deploy:full": "npm run build && npm run deploy",
    "full": "npm install && npm run build && npm run deploy:full"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "17.0.1",
    "postcss-cli": "^9.0.0",
    "rollup": "^3.0.0",
    "html-minifier": "^4.0.0",
    "gh-pages": "^5.0.0"
  }
}
